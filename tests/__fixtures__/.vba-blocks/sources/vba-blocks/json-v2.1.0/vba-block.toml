[package]
name = "json"
version = "2.1.0"
authors = ["Tim Hall <tim.hall.engr@gmail.com> (https://github.com/timhall)"]
license = "MIT"

[src]
JsonConverter = "JsonConverter.bas"

[dependencies]
dictionary = "^1"
