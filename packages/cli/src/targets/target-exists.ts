import { Config } from '../config';
import { Target } from '../manifest';

export default async function targetExists(
  config: Config,
  target: Target
): Promise<boolean> {
  // TODO
  return false;
}
