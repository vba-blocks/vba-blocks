import { Config } from '../config';
import { Target } from '../manifest';

export default async function createTarget(config: Config, target: Target) {
  // TODO
  // 1. zip target folder into build
}
